class SkylabArray {
  index = 0;

  constructor(index0, index1, index2, index3) {
    this.index0 = index0;
    this.index1 = index1;
    this.indx2 = index3; 
  }
}

const infinity() => {
  let index= 0;

  while (<arg.length) {
        return index++;
    }
}
infinity(); 

const numbers = new SkylabArray(3);

console.log(numbers(this.element));

/*
[element0, element1];
new skylabArrayList(element0, element1, elementN);
console.log(skylabArrayList);


const array = [0, 2, 3];
console.log(typeof array);

*/
